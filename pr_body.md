## 関連する Issue

- #41

## 変更内容

- `analytics_events` テーブルの定義を追加しました。
- 各種イベントを記録できるように、`event_name`, `station_id`, `campus_id`, `metadata_json`, `environment` などのカラムを定義しました。
- `environment` には 'production' または 'development' のみ許可するCHECK制約を追加しています。
- Drizzle Kitで対応するマイグレーションファイルを生成しました。

## なぜこの変更が必要か

- マップの利用状況（閲覧されたか、どの給水機がタップされたか等）を記録・分析し、将来的な改善に活かすための土台となるテーブルが必要なためです。

## 変更の検証方法

1. `apps/lib/db/schema.ts` にテーブル定義が正しく追加されていることを確認する。
2. 自動生成されたマイグレーションファイル（`0005_...`）のSQLが意図した構造になっているか確認する。
3. CIのLintや型チェック、Prettierフォーマットが通過していることを確認する。

## スクリーンショット/デモ

なし（バックエンド・DBスキーマ変更のみのため）

## チェックリスト

- [x] 関連する Issue へのリンクが記述されているか
- [x] コードがプロジェクトのコーディング規約に従っているか
- [x] 新しいテストが追加されたか、または既存のテストがパスしているか
- [x] ドキュメントが更新されたか (必要な場合)
- [x] セルフレビューを行ったか

## レビュアーへの特記事項

- 前回の `feature/stations-shiomi` 等の変更が含まれて表示される場合があります。このPRはStacked PRとして作成しています。

Close #41
